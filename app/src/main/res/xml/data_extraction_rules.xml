<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- Exclude sensitive data from cloud backup -->
        <exclude domain="sharedpref" path="voiddrop_preferences.xml"/>
        <exclude domain="database" path="voiddrop.db"/>
    </cloud-backup>
    <device-transfer>
        <!-- Allow device transfer for user data -->
        <include domain="sharedpref" path="voiddrop_preferences.xml"/>
    </device-transfer>
</data-extraction-rules>